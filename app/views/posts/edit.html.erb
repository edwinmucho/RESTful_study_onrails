<h1>Edit Story</h1>
<%= form_tag(post_path(@post.id), method: "put") do%>
<%= text_field_tag(:image_url, @post.img_url)%><br>
<%= text_area_tag(:story, @post.story)%><br>
<%= submit_tag("Edit")%>
<%end%>
